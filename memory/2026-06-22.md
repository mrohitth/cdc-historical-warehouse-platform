
## 🔒 Mitty Security Audit — 2026-06-22 03:00 UTC

**Host:** Ubuntu 26.04 LTS (Resolute Raccoon) · Linux 7.0.0-15-generic x86_64
**Gateway:** MRIWANTCHICKEN · OpenClaw stable channel

### ✅ SSH Check
- SSH config valid, localhost accessible (user: mathew, port 22)

### ⚠️ Firewall Check
- **No UFW or firewalld detected** — host has no active firewall daemon
- Listening ports (local-only):
  - `127.0.0.1:18789` — OpenClaw gateway (node, loopback only ✅)
  - `127.0.0.1:631` — CUPS print service (loopback only ✅)
  - `127.0.0.53/54:53` — systemd-resolved (loopback ✅)
- All services bound to loopback — good network posture

### ✅ Gateway Check
- Gateway probe: **timeout** (expected for deep probe on loopback bind)
- Gateway bind: **loopback only** (`127.0.0.1:[18789]` and `[::1]:18789`) ✅
- trustedProxies: empty — safe since loopback-only bind
- Control UI: local-only ✅
- Webhooks: disabled ✅
- Browser control: enabled (personal assistant mode)
- Plugins: 1 unpinned npm spec — `brave (@openclaw/brave-plugin)` ⚠️

### ⚠️ Cron Health
- All scheduled jobs running OK
- "Mitty Security Audit" cron (5de42e46) — last ran 1d ago, next in 4m ✅
- Notable: "Credential Leak Scanner" every 6h running ✅

### ✅ RAM / Disk
- RAM: 6.7Gi total · 2.7Gi used · 4.0Gi available ✅
- Swap: 19Gi total · 628Ki used ✅
- Root FS: 80Gi total · 44G used · 33G avail (58%) ✅

### ⚠️ Filesystem / System
- Disk usage 58% — healthy
- No filesystem anomalies noted

### 🔶 Actionable Findings
1. **No host firewall daemon** — consider enabling UFW or nftables
2. **OpenClaw update available** — `pnpm`/npm update to 2026.6.9
3. **brave plugin unpinned** — pin `@openclaw/brave-plugin` to exact version
4. **gateway.probe_failed** — deep probe times out (likely loopback-only bind); `openclaw status --all` if remote access issues arise
5. **Telegram groupPolicy=open** — potential multi-user setup flagged; confirm intentional

### Commands Run
```
openclaw security audit --deep
openclaw update status
openclaw status
ss -ltnp
df -h / && free -h
cat /etc/os-release
ssh -G localhost
ufw status verbose
openclaw cron list
```
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] TrendScout 2026-06-22 — new-parent-exhaustion pipeline complete (guide 36KB + checklist 21KB, checkout: https://mrohitth.lemonsqueezy.com/checkout/buy/new-parent-exhaustion)
- [x] [K] MarketBot scan (cron)
- [x] [K] PDF Pipeline Daily — full run completed at 11:04 ET
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)

## 🔒 Mitty Security Audit — 2026-06-22 23:00 EDT

### SSH Check
- No SSH ports detected listening on this host.

### Firewall Check
- `ufw`, `firewall-cmd`, and `iptables` returned no output — firewall tool status indeterminate (may not be installed or requires elevated permissions).

### Gateway Check
- **Status:** Running (pid 105849, state active)
- **Bind:** loopback only (127.0.0.1:18789) ✅ — not publicly exposed
- **Connectivity probe:** ok ✅
- **Service:** systemd user service, enabled
- **⚠️ Issues:**
  - Gateway service PATH includes version managers (`nvm`) — recommended to use minimal PATH
  - Service uses Node from version manager — can break after upgrades
  - Recommend: run `openclaw doctor` and migrate to System Node 22 LTS

### OpenClaw Security Audit
- **⚠️ WARN:** `channels.telegram.groupPolicy="open"` — recommend allowlist mode
- **⚠️ WARN:** `gateway.trusted_proxies_missing` — loopback-only for now (fine), but if reverse proxy added, configure trustedProxies
- **🔴 HIGH:** `security.trust_model.multi_user_heuristic` — personal-assistant model warning. Multiple users can access gateway. Runtime/fs tools exposed in non-sandboxed contexts (`agents.defaults sandbox=off`, `fs.workspaceOnly=false`). If users are mutually untrusted, consider splitting trust boundaries.
- **⚠️ WARN:** `plugins.installs_unpinned_npm_specs` — `brave` plugin unpinned
- **INFO:** Elevated tools enabled, browser control enabled, webhooks disabled

### Cron Health
- All 10 cron jobs status: `ok` or `running` ✅
- No failed jobs detected
- This audit job (Mitty Security Audit) ran successfully

### RAM Check
- Total: 6.7Gi | Used: 2.7Gi (40%) | Available: 3.9Gi ✅
- Swap: 19Gi total, virtually unused (912Ki) ✅

### Filesystem Check
- `/dev/nvme0n1p6` (root): 80G total, 44G used (58%) ✅ — healthy
- `/dev/nvme0n1p1` (boot/efi): 256M, 38M used (15%) ✅

### Key Actions Recommended
1. Run `openclaw doctor` to fix PATH/Node version manager issues
2. Set `channels.telegram.groupPolicy="allowlist"` if group access should be restricted
3. Consider `agents.defaults.sandbox.mode="all"` if multiple untrusted users share this gateway
4. Pin brave plugin to exact version: `@openclaw/brave-plugin@x.x.x`


## Kaizen Analysis — 2026-06-22
- Session bloat ghost alert: SESSIONS=439MB triggers HEALTH_CRITICAL every minute (SESSION_WARN=1, threshold 150MB). Root cause: 48 accumulated sessions + trajectory files. Not a real issue — RAM 38%, disk 58%, no swap pressure. Fix: raise SESSION_WARN threshold in health_pulse to 500MB or enable session auto-cleanup more aggressively.
- DeepSeek PAYG usage: main agent session (agent:main:main) used deepseek-v4-pro at 3% of 1049k context yesterday — likely a manual model switch. Cost unknown but likely small. All cron Kaizen sessions used MiniMax-M2.7 (subscription) as expected.
- SSH: no failed login attempts detected.
- Cron: clean, no errors. All 10 cron jobs OK.
- Firewall: no UFW/firewalld running — but all services are loopback-only so exposure is minimal.

