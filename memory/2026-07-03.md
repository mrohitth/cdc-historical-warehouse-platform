# 2026-07-03 - Mitty Security Audit

## Audit Run @ 2026-07-03 03:00 UTC (cron job)

### Health Pulse (mitty_health_pulse.sh)
```
[HEALTH_CRITICAL] RAM=42% | DISK=58% | SESSIONS=456M | BAK=0 | ⚠️
```

### OpenClaw Security Audit --deep
**Summary: 3 CRITICAL · 4 WARN · 1 INFO**

#### CRITICAL Issues
1. **Open groupPolicy + elevated tools** — `channels.telegram.groupPolicy="open"` with `tools.elevated` enabled → prompt injection risk in Telegram groups
   - Fix: Set `groupPolicy="allowlist"`, tighten elevated allowlists
2. **Open groupPolicy + runtime/filesystem tools exposed** — sandbox=off, runtime=[exec, process], fs=[read, write, edit, apply_patch], fs.workspaceOnly=false
   - Fix: Use `tools.profile="messaging"` or deny group:runtime/group:fs for open groups; set `agents.defaults.sandbox.mode="all"`
3. **Telegram security warning** — open groupPolicy allows any member in allowed groups to trigger mention-gated commands
   - Fix: `channels.telegram.groupPolicy="allowlist"` + group allowlist

#### WARN Issues
4. **Reverse proxy headers not trusted** — `gateway.bind=loopback`, `trustedProxies` empty
   - Fix: Set `gateway.trustedProxies` or keep Control UI local-only
5. **Potential multi-user setup detected** — Telegram open group + exposed runtime/fs tools without full sandboxing
   - Fix: If untrusted multi-user, split trust boundaries; else set `agents.defaults.sandbox.mode="all"`, `tools.fs.workspaceOnly=true`
6. **Unpinned npm plugin** — `brave (@openclaw/brave-plugin)` not pinned
   - Fix: Pin to exact version `@openclaw/brave-plugin@1.2.3`
7. **Gateway probe failed (deep)** — timeout during deep probe
   - Fix: `openclaw status --all` to debug

#### INFO
- Attack surface: groups=open(1), elevated tools=enabled, browser control=enabled
- Trust model: personal assistant (one trusted operator boundary)

### System Status
- **Gateway**: local loopback, ws://127.0.0.1:18789, reachable 67ms
- **Tailscale**: off
- **Update available**: npm 2026.6.11 (stable channel)
- **RAM**: 42%, **Disk**: 58%
- **Sessions**: 18 active
- **Tasks**: 1 active, 4 issues, 286 tracked
- **Backup**: not configured (BAK=0)

### Commands Run
- `bash mitty_health_pulse.sh`
- `openclaw security audit --deep`
- `openclaw update status`
- `openclaw status --deep`

### Recommendations
1. Address Telegram groupPolicy CRITICALs (switch to allowlist or confirm intentional open group)
2. Run `openclaw update` to patch to 2026.6.11
3. Pin brave plugin to exact version
4. Configure backups (BAK=0)
5. Investigate deep probe timeout
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)
- [SKIPPED] [K] PDF Pipeline Daily — no high-value trends found by TrendScout (2026-07-03). Reddit 403s blocked raw quote collection. Pipeline exited gracefully at $(date +%H:%M ET)
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)

---

## Audit Run @ 2026-07-04 03:00 UTC (cron job)

### SSH Check
- **Status**: SSH daemon not running on this host (sshd not found in path)
- **Config**: No SSH config issues detected
- **Result**: ✅ No SSH exposure

### Firewall Check
- **Status**: UFW not available (not a ufw-managed system, or not Ubuntu/Debian)
- **Listening ports**:
  - `127.0.0.1:18789` — OpenClaw Gateway (local loopback only) ✅
  - `127.0.0.53:53` / `127.0.0.54:53` — DNS resolver (local)
  - `127.0.0.1:631` — CUPS print service (local)
- **Result**: ✅ No exposed services; gateway bound to loopback only

### Gateway Check
- **Gateway**: local loopback · ws://127.0.0.1:18789 · reachable 52ms ✅
- **Dashboard**: http://127.0.0.1:18789/ (local only)
- **Tailscale exposure**: off
- **Gateway service**: systemd user · enabled · running (pid 105849)
- **Update available**: yes — pnpm/npm · 2026.6.11

### OpenClaw Security Audit (read-only)
**Summary: 3 CRITICAL · 4 WARN · 1 INFO**

#### CRITICAL Issues
1. **Open groupPolicy + elevated tools** — `channels.telegram.groupPolicy="open"` with `tools.elevated` enabled → prompt injection risk in Telegram groups
2. **Open groupPolicy + runtime/filesystem tools** — sandbox=off, runtime=[exec, process], fs=[read, write, edit, apply_patch], fs.workspaceOnly=false
3. **Telegram open groupPolicy** — any member can trigger mention-gated commands

#### WARN Issues
4. Reverse proxy headers not trusted (loopback bind — acceptable)
5. Potential multi-user setup detected (personal assistant mode — acceptable if single user)
6. Unpinned npm plugin: `brave (@openclaw/brave-plugin)`
7. Gateway deep probe skipped (timeout)

### Cron Health
- **Mitty Security Audit** (this job): running ✅
- **Bitty Budget Update**: ok · next in 26m
- **Mitty Health Pulse**: ok · next in 39m
- **Credential Leak Scanner**: ok · next in 3h
- **Session BAK Auto-Clean**: ok · next in 3h
- **Morning Kaizen**: ok · next in 10h
- **Capital Pilot Daily**: ok · next in 10h
- **TrendScout Daily**: ok · next in 11h
- **You are Kitty**: ok · next in 2d
- All cron jobs: ✅ healthy

### RAM Check
- **Memory**: 6.7 GiB total
- **Used**: 2.9 GiB (42%) ✅
- **Available**: 3.8 GiB
- **Swap**: 19 GiB total · 23 MiB used (0%) ✅

### Filesystem Check
- **Root**: `/dev/nvme0n1p6` · 80 GiB total · 44 GiB used · 32 GiB free (58%) ✅
- No filesystems over 80% ✅

### Commands Executed
- `openclaw security audit`
- `openclaw status`
- `openclaw cron list`
- `ss -ltnp`
- `free -h`
- `df -h`

### Key Recommendations
1. ⚠️ **Address Telegram groupPolicy CRITICALs** — set `groupPolicy="allowlist"` if groups are meant to be restricted
2. ⚠️ **Update OpenClaw** — npm 2026.6.11 available
3. ⚠️ **Pin brave plugin** to exact version for supply-chain stability
4. ✅ No critical system-level exposures found
5. ✅ Gateway properly bound to loopback — not internet-facing



## Kaizen Analysis (2026-07-04 morning)

**Session bloat — 460MB total session storage**
- Root cause: Trajectory files (`.trajectory.jsonl`) and session `.jsonl` files grow large from long context windows and verbose metadata. Main session alone is ~460MB.
- Fix: Lower session compaction threshold or increase the SESSION_WARN trigger from 150MB to 300MB if 460MB is stable. Or add a cron that prunes sessions older than 7 days.
- Priority: Low (no RAM/disk impact yet, but watch if it climbs past 600MB)

**HEALTH_CRITICAL false alarm — session size only**
- Root cause: Health pulse triggers HEALTH_CRITICAL on ANY warning flag. RAM=42% and Disk=58% are healthy. Only SESSION_WARN=1 is active.
- Fix: Refactor health pulse to only flag HEALTH_CRITICAL when RAM >75%, Disk >85%, or multiple concurrent warnings. Session size alone should be SESSION_WARN, not CRITICAL.
- Priority: Medium (alert fatigue erodes real signal)

**OpenClaw update available**
- Version 2026.6.11 ready via npm/pnpm. Current: 2026.5.4. No security advisories noted.
- Priority: Low

**No DeepSeek PAYG calls detected**
- All sessions used MiniMax-M2.7 only. deepseek plugin is disabled. No cost exposure.

