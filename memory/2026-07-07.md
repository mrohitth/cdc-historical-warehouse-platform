# 2026-07-07

## Mitty Security Audit (scheduled cron)

**Time:** 2026-07-07 03:00 UTC / 2026-07-06 23:00 ET

### SSH Check
- No SSH service detected listening on standard port 22.
- No ufw/firewalld firewall detected (likely running permissive or containerized).

### Firewall Check
- No ufw or firewalld active on this host.
- Gateway bound to loopback only (127.0.0.1:18789) — not exposed externally.

### Gateway Check
- Gateway: **running** (pid 105849, systemd user service, state active)
- Bind: loopback only (local access only, not internet-facing)
- Tailscale: off
- Dashboard: http://127.0.0.1:18789/
- **Issues flagged:**
  - Gateway service PATH includes version managers (nvm) — can break after upgrades
  - Service config uses Node from nvm instead of system Node 22 LTS
  - Recommended: migrate to System Node 22 LTS before nvm-managed Node breaks
  - Run `openclaw doctor` / `openclaw doctor --repair` to fix

### OpenClaw Security Audit (--deep)
- **Trust model:** personal assistant, single trusted operator boundary
- **Browser control:** enabled
- **Tools.elevated:** enabled
- **Groups:** open=1, allowlist=0
- **Plugin issue:** brave plugin install spec is unpinned (supply-chain risk)
- **Gateway probe:** failed (timeout) — likely due to deep probe timing out loopback
- **Fix needed:** `openclaw security audit --fix` for plugin pinning

### Update Status
- **Channel:** stable
- **Update available:** npm 2026.6.11
- Run: `pnpm update -g openclaw` or `npm update -g openclaw`

### RAM Check
- Total: 6.7 GiB | Used: 2.9 GiB | Available: 3.8 GiB | Swap: 19 GiB (23 MiB used)
- **Status:** Healthy

### Disk / Filesystem
- /dev/nvme0n1p6 — 80G total, 45G used, 32G avail, 59% used
- **Status:** Healthy

### Uptime
- 41 days, 3 hours — no recent reboots

### Active Tasks
- 1 running task, 2 issues, 0 queued

### Cron Note
- `mitty_security_audit.sh` script referenced in cron does not exist at path.
- Replaced with direct healthcheck audit commands.

### Commands Run
- `openclaw security audit --deep`
- `openclaw update status`
- `openclaw gateway status`
- `openclaw status`
- `ss -ltnp`, `free -h`, `df -h / /home`, `uptime`
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] TrendScout 2026-07-07 — diy-ant-control-budget-guide_v1, cat-inappropriate-elimination-vet-cleared_v1, toddler-sleep-regression-survival_v1 pipeline complete. PDFs + checklists synced to GitHub. LS deploy: manual (API read-only, manifests generated).
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] PDF Pipeline Daily — full run completed at 11:01 ET
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)

## 🔒 Security Audit — Mitty (2026-07-08 03:00 UTC)

### Host
- OS: Ubuntu 26.04 LTS (Resolute Raccoon), Linux 7.0.0-15-generic x86_64
- OpenClaw update: **available** (npm 2026.6.11, run `openclaw update`)

### Network / Attack Surface
- Listening: 127.0.0.1:18789 (node/OpenClaw), 127.0.0.53%lo:53 (dnsmasq), 127.0.0.1:631 (cups)
- **No SSH server** listening on port 22 ✓
- **No ufw/firewalld/nftables ruleset found** (systemd-resolved handles DNS on localhost)
- Gateway bind: localhost only (127.0.0.1) ✓
- OpenClaw groups: open=1, allowlist=0

### OpenClaw Security (--deep)
- `plugins.installs_unpinned_npm_specs` — Brave plugin uses unpinned spec; pin to exact version for supply-chain stability
- `tools.elevated: enabled` — risky if gateway is exposed; confirm intent
- `browser control: enabled` — recommend 2FA on important accounts (hardware keys preferred)
- `gateway.probe_failed` — gateway deep probe timed out (expected if gateway is localhost-only)
- Trust model: personal assistant (one trusted operator) ✓

### Resources
- RAM: 6.7Gi total, 3.0Gi used, 704Mi free, 3.7Gi available
- Swap: 19Gi total, 4.1Mi used ✓
- Disk: 80G / (nvme), 45G used, 32G free (59%)

### Active Cron Jobs (11 total)
- Mitty Security Audit (daily 11PM ET) ← this run
- Bitty Budget Update (daily 11:30PM ET)
- Mitty Health Pulse (hourly)
- Credential Leak Scanner (every 6h)
- Session BAK Auto-Clean (daily 2AM ET)
- Kitty Morning Kaizen (weekdays 9AM ET)
- Capital Pilot Daily Brief (weekdays 9:30AM ET)
- You are Kitty / Kaizen mid-day (weekdays 9:45AM-4:45PM ET)
- TrendScout Daily (daily 10AM ET)
- PDF Pipeline Daily (daily 11AM ET)

### Flags to Address
1. **Run `openclaw update`** — new version available
2. **Pin brave plugin** to exact version (`@openclaw/brave-plugin@x.y.z`)
3. **No host firewall detected** — confirm if ufw should be enabled (machine is cloud/headless, SSH not even running, so low exposure)


## Kaizen Analysis — 2026-07-08 Morning (Jul 7 data)

**Health Pulse (last 24h):** RAM 43%, Disk 59%, Sessions 468MB | HEALTH_CRITICAL repeated (false alarm — session-size threshold 150MB, not a real issue)
**SSH:** inactive (port 22 closed) | **Firewall:** inactive (no ufw/firewalld, machine is cloud/headless with gateway on loopback — low exposure) | **Gateway:** OK
**Cron:** clean (11 active crons, no failed runs) | **Consecutive errors:** 0
**DeepSeek PAYG:** None — all MiniMax M2.7 (subscription)

**Kaizen Idea #1 — HEALTH_CRITICAL false alarm from session-size threshold**
Root cause: health_pulse triggers HEALTH_CRITICAL when SESSIONS >150MB. The current session footprint is 468MB (accumulated trajectory/session logs). This is NOT a real problem — RAM is 43%, system is healthy. But the repeated HEALTH_CRITICAL pollutes logs and could mask real issues.
Fix: Raise the SESSION_WARN threshold in health_pulse to 400MB+ to match reality, or add a recurring session trim cron to keep session files leaner.

**Kaizen Idea #2 — Run openclaw update (available since Jun 2)**
Root cause: OpenClaw 2026.6.11 has been available for over a month but the gateway is still on 2026.5.4. The security audit already flagged this.
Fix: Run `openclaw update` when convenient.

