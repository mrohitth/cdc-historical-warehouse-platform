
## 🔒 Mitty Security Audit — 2026-06-16 03:00 UTC

### System
- **OS:** Ubuntu 26.04 LTS (Resolute Raccoon), Linux 7.0.0-15-generic x86_64
- **RAM:** 6.7Gi total · 2.6Gi used · 4.1Gi available
- **Disk:** 80GB NVMe · 43GB used (57%) · 34GB available
- **Swap:** 19GB · 912Ki used

### Gateway
- **Status:** Running (pid 105849, loopback-only on 127.0.0.1:18789)
- **Dashboard:** http://127.0.0.1:18789/
- **⚠️ Issues:** Service PATH uses version manager (nvm); recommend System Node 22 LTS
- **Update:** npm 2026.6.6 available

### Listening Ports
- 127.0.0.1:18789 (OpenClaw gateway, node)
- 127.0.0.53/54:53 (DNS resolver)
- 127.0.0.1:631 (CUPS printer)

### Firewall
- No ufw/firewalld detected (default Ubuntu iptables likely running)

### OpenClaw Security Audit — 3 CRITICAL · 4 WARN · 1 INFO

**CRITICAL:**
1. `channels.telegram.groupPolicy="open"` + `tools.elevated` enabled → prompt injection risk in Telegram groups
2. `channels.telegram.groupPolicy="open"` + runtime/filesystem tools exposed → same risk vector
3. Telegram groupPolicy="open" allows any group member to trigger mentions

**WARN:**
1. `gateway.trustedProxies` empty (loopback bind — acceptable if not behind proxy)
2. Potential multi-user setup detected — sandboxing not enforced for exposed agents
3. `@openclaw/brave-plugin` has unpinned npm spec
4. Gateway probe failed (deep check timeout)

**INFO:**
- Attack surface: 1 open group, elevated tools enabled, browser control enabled
- Trust model: personal assistant (single operator boundary)

### Cron Jobs
- (run via external cron trigger — this audit itself)

### Action Items
- [ ] Set `channels.telegram.groupPolicy="allowlist"` and configure `groupAllowFrom`
- [ ] Pin `@openclaw/brave-plugin` to exact version
- [ ] Run `openclaw update` to get npm 2026.6.6
- [ ] Consider migrating gateway service to System Node 22 LTS (avoid nvm PATH in systemd unit)
- [ ] Re-run `openclaw security audit --deep` after fixing Telegram groupPolicy
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] TrendScout 2026-06-16 — 4-month-sleep-regression-guide pipeline complete (guide + checklist PDFs, GitHub synced, LS manifest generated)
- [x] [K] PDF Pipeline Daily — full run completed at 11:07 ET
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)


## Kaizen Analysis — 2026-06-17 (analyzing 2026-06-16)

### Health Pulse (24h window Jun 16-17)
- RAM: 38-39% — clean, no spikes
- Disk: 57% — clean, no越过 85%
- Sessions: 431MB — SESSION_WARN flagged continuously
- HEALTH_CRITICAL triggered every cycle — but RAM/disk/session WARNs all 0; the CRITICAL is noise due to session size (431MB) exceeding the 150MB threshold
- No BAK (backup count) activity

### Security Audit (from memory log)
- 3 CRITICAL: Telegram groupPolicy="open" + elevated tools = prompt injection risk in Telegram groups
- 4 WARN: empty trustedProxies, multi-user sandboxing gap, unpinned brave-plugin, gateway probe timeout
- Telegram groupPolicy="open" remains unresolved from prior audit

### Cron / Sessions
- Multiple MarketBot scans + TrendScout + PDF pipeline ran cleanly on Jun 16
- No failed cron jobs detected
- Sessions from Jun 16 all used MiniMax-M2.7 — no DeepSeek PAYG calls

### Kaizen Ideas
1. Fix HEALTH_CRITICAL noise — session threshold (150MB) too low for 431MB actual on 6.7Gi RAM. Bump threshold to 500MB or remove SESSION_WARN from CRITICAL trigger
2. Telegram groupPolicy — set to "allowlist" + configure groupAllowFrom to close prompt injection surface (still open from prior audit)
3. Gateway nvm PATH — migrate to System Node 22 LTS to avoid version manager dependency in systemd unit

### DeepSeek PAYG Audit
No DeepSeek PAYG calls on Jun 16. All model calls: MiniMax-M2.7 (subscription). The deepseek plugin is disabled (enabled: false).

