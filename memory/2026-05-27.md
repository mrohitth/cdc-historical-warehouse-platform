# 2026-05-27 - Mitty Security Audit

## Time
Ran at 23:00 EDT (cron job `5de42e46-7449-4d26-932d-4890e26cce6e`)

## What ran
- `openclaw security audit --deep`
- `openclaw status` (gateway + channel overview)
- Quick system checks (uptime, RAM, disk)

## Findings

### OpenClaw Security Audit (--deep)
| Issue | Severity | Fix |
|---|---|---|
| `gateway.trustedProxies` empty | ⚠️ Medium | Set to proxy IPs if exposing Control UI via reverse proxy |
| `security.trust_model.multi_user_heuristic` | ⚠️ Info | Personal-assistant model; Telegram group allowlist active |
| Runtime/process tools exposed without full sandboxing | ⚠️ Info | agents.defaults sandbox=off; fs.workspaceOnly=false — intentional for personal use |
| `plugins.installs_unpinned_npm_specs` | ⚠️ Low | Brave plugin unpinned spec — pin for supply-chain stability |
| `gateway.probe_failed` (deep) | 🔴 Critical | Probe timeout — needs `openclaw status --all` debug |

### System
- **Uptime:** 1 day, 3h 21min
- **RAM:** 2.2Gi used / 4.5Gi available — healthy
- **Disk:** 42G/80G used on `/` (56%) — healthy
- **SSH:** Not detected listening on standard port (local only or inactive)
- **Gateway:** Telegram channel OK, accounts 1/1

### Attack Surface
- Groups: open=0, allowlist=1 (Telegram)
- Tools elevated: enabled
- Browser control: enabled
- Trust model: personal-assistant (one trusted operator)

## Action Items
- [ ] Investigate `gateway.probe_failed` — run `openclaw status --all`
- [ ] Pin Brave plugin npm spec
- [ ] If using reverse proxy for Control UI, configure `gateway.trustedProxies`

## Kaizen Morning — 2026-05-28
**Health pulse (24h):** RAM=32-33%, Disk=56%, Sessions=425M | SESSION_WARN triggered HEALTH_CRITICAL 50x — but RAM/disk clean
**Kaizen idea #1 — Session bloat alert noise:** Sessions=425M exceeded 150MB threshold, causing HEALTH_CRITICAL spam. Root cause: openclaw agent session storage growing large without cleanup. Fix: Implement session pruning cron — add `find ~/.openclaw/agents/main/sessions/ -name "*.jsonl" -mtime +3 -delete` or set sessions.auto_checkpoint=false and prune checkpoints aggressively.
**Kaizen idea #2 — Threshold calibration:** HEALTH_CRITICAL fires on SESSION_WARN alone even when RAM/disk are healthy. Consider adjusting health thresholds: raise session size warn threshold from 150M to 300M, or weight warnings individually rather than lifting to CRITICAL.
**SSH:** inactive | **Firewall:** active | **Gateway:** WARNING (probe_failed noted)
**Cron:** 3 alerts from 2026-05-27 audit | **Consecutive errors:** 0
**Cost Audit:** No DeepSeek PAYG — all MiniMax M2.7 (subscription)

