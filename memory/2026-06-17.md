# 2026-06-17 — Mitty Security Audit Log

## Audit Run @ 2026-06-18 03:00 UTC

### 🔒 Security Audit Findings

**SSH Check:** Port 22 (SSH) not in listening ports — either disabled or not running.

**Firewall Check:** ⚠️ No managed firewall detected (ufw/firewalld inactive). Host may have unfiltered exposure.

**Gateway Check:**
- Gateway: running on loopback only (127.0.0.1:18789), probe OK ✅
- Service config warnings: version manager PATH in systemd service, System Node 22 LTS not found
- Update available: npm 2026.6.8

**OpenClaw Security Audit (--deep):**
- ⚠️ `gateway.bind=loopback` + no `trustedProxies` set (safe if intentionally local-only)
- ⚠️ Potential multi-user setup: `channels.telegram.groupPolicy="open"` — tools exposed without full sandboxing
- ⚠️ `plugins.installs_unpinned_npm_specs`: Brave plugin uses unpinned npm spec (`@openclaw/brave-plugin`)
- ⚠️ `gateway.probe_failed`: deep probe timed out
- ✅ `trust_model=personal-assistant` (one trusted operator — appropriate)

**Cron Health:** Not checked in this run.

**RAM Check:** 38% used ✅ (threshold: 75%)

**Disk Check:** 57% used ✅ (threshold: 85%)

**Sessions:** ⚠️ 434M — exceeds 100MB threshold (flagged HEALTH_CRITICAL by mitty_health_pulse.sh)

**BAK Count:** 0 ✅

### Commands Run
- `bash mitty_health_pulse.sh` → HEALTH_CRITICAL (sessions > 100MB)
- `openclaw security audit --deep` → 4 warnings, 0 critical
- `openclaw gateway status` → running, loopback-only
- `openclaw update status` → update available
- `ss -ltnp` → listening ports listed
- `ufw status verbose` → no managed firewall

### Recommendations
1. Pin Brave plugin npm spec: `@openclaw/brave-plugin@<version>`
2. Consider enabling a firewall (ufw) if exposed to untrusted networks
3. Sessions folder growing large (434M) — consider periodic cleanup
4. Run `openclaw update` to get 2026.6.8
5. Fix systemd service PATH for long-term stability (use system Node)
6. Re-run `openclaw security audit --deep` after addressing probe failure


## Kaizen Analysis — 2026-06-18 Morning

Health pulse over last 24h: RAM steady 38-39% (no spikes), Disk 57% (no越过), Sessions flagged at 434M. HEALTH_CRITICAL triggered solely by sessions >100MB threshold — not a real problem.

Security audit findings from 2026-06-18 03:00 UTC: SSH disabled, no managed firewall (ufw inactive), 4 security warnings (groupPolicy open, unpinned Brave plugin, probe timeout, systemd PATH). Gateway running on loopback only.

DeepSeek PAYG audit: No DeepSeek PAYG calls. All sessions used MiniMax-M2.7 (subscription). DeepSeek plugin disabled.

Kaizen ideas:
1. Sessions HEALTH_CRITICAL false alarm — mitty_health_pulse.sh threshold of 100MB is too tight for this host. Sessions at 434M with 38% RAM and 57% disk is healthy. Raise threshold to 500MB in health pulse script or adjust alert logic to weight RAM/disk before session size.
2. No firewall detected — if host is exposed to untrusted networks, enable ufw. If loopback-only is intentional (local-only gateway), this is low risk but should be documented.
3. Brave plugin uses unpinned npm spec — pin to `@openclaw/brave-plugin@<version>` to prevent unexpected updates.
4. Group policy open + tools exposed — if Telegram group access is intended for trusted users only, this is fine. If open groups are unexpected, tighten `channels.telegram.groupPolicy`.

No DeepSeek PAYG cost incurred. All MiniMax M2.7 (subscription).

