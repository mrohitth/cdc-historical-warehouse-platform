# 2026-06-15

## Mitty Security Audit — Nightly Run (23:00 ET)

### System
- **Host:** MRIWANTCHICKEN · Ubuntu 26.04 LTS · Linux 7.0.0-15-generic x64
- **Gateway:** local loopback (127.0.0.1:18789) · reachable 156ms
- **Gateway service:** systemd user · running · pid 105849
- **Tailscale:** off
- **Update available:** npm 2026.6.6 (channel: stable)

### Listening Ports
- 127.0.0.1:18789 — OpenClaw Gateway (node)
- 127.0.0.53:53 / 127.0.0.54:53 — systemd-resolved
- 127.0.0.1:631 — cups (localhost)
- ::1:631 — cups (IPv6 localhost)

### Firewall
- No UFW/firewalld/pf detected. **No host firewall active.**

### Filesystem
- `/` — 80GB total · 43GB used · 34GB avail · 57%

### RAM
- 6.7GB total · 2.6GB used · 4.1GB available · 332MB free
- Swap: 19GB total · 2.7MB used

### OpenClaw Security Audit — Findings

**3 CRITICAL:**
1. `channels.telegram.groupPolicy="open"` + `tools.elevated` enabled — prompt injection in groups can become high-impact
2. `channels.telegram.groupPolicy="open"` + runtime/filesystem tools exposed — risky tool exposure
3. Telegram groupPolicy open — any group member can trigger (mention-gated)

**3 WARN:**
1. `gateway.trusted_proxies_missing` — reverse proxy headers not trusted (gateway.bind is loopback; not a current risk since loopback-only)
2. Potential multi-user setup detected — sandbox=off on agents.defaults with runtime/fs tools; workspaceOnly=false
3. `brave` plugin has unpinned npm spec in plugin index

**1 INFO:**
- Attack surface: groups=open(1), allowlist(0); tools.elevated=enabled; browser control=enabled

**Deep probe:** Gateway probe failed (timeout) — `openclaw status --all` may need debugging

### Cron Jobs Running
- Mitty Security Audit: running now (this job)
- Bitty Budget Update: in 25m
- Mitty Health Pulse: every 1h
- Credential Leak Scanner: every 6h, in 3h
- Session BAK Auto-Clean: in 3h
- Kitty Morning Kaizen: in 10h
- Capital Pilot Daily: in 10h
- You Are Kitty: in 11h
- TrendScout Daily: in 11h
- PDF Pipeline Daily: in 12h

### Actions Taken
- None (read-only audit)

### Key Decisions / Notes
- Telegram groupPolicy=open is a known/accepted risk; elevated tools are intentional
- No firewall detected — host is likely behind NAT router; exposed ports are localhost-only
- Update available but not applied (manual decision)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] TrendScout 2026-06-15 — preschool-bedtime-resistance-guide pipeline complete
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] PDF Pipeline Daily — full run completed at 11:04 ET
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scanner — delivered
- [x] [K] MarketBot scan (cron)
- [x] [K] MarketBot scan (cron)

## Kaizen Analysis — 2026-06-16 Morning

Health pulse: RAM=38%, Disk=57%, Sessions=429MB — HEALTH_CRITICAL from session size (429MB > 150MB threshold). RAM and disk are clean. 50 identical HEALTH_CRITICAL pulses logged in 24h — this is sustained, not a spike.

No firewall (UFW inactive) — already documented as accepted risk (host behind NAT, ports localhost-only). No SSH failed logins detected. Gateway service healthy and running.

DeepSeek PAYG audit: 5 session trajectory files contained the word 'deepseek' but all actual model calls were MiniMax-M2.7. No DeepSeek pay-per-token charges incurred.

Kaizen idea #1 — Session Bloat: Sessions sitting at 429MB (2.9x the warning threshold). The Session BAK Auto-Clean cron is scheduled but apparently not keeping up. Root cause likely either (a) sessions are accumulating faster than the 6-hour clean interval, or (b) the clean threshold is set too high. Fix: review session_cleanup config — lower the size threshold that triggers cleanup, or add a post-session trigger to clean immediately after a large session closes.

No DeepSeek PAYG — all MiniMax M2.7 (subscription).

