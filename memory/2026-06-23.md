# 2026-06-23 Daily Notes

## Mitty Security Audit — Full Flow (23:00 UTC)

### 🔒 Security Audit Findings

**SSH Check:** No SSH server daemon running. ssh-agent session agent active (user: mathew). No remote SSH access detected.

**Firewall Check:** No managed firewall (ufw/firewalld) active. iptables empty/default. Gateway bound to loopback only (127.0.0.1:18789) — not exposed.

**Gateway Check:**
- Gateway: reachable (154ms)
- Event loop: healthy (p99 22ms, util 0.067)
- Telegram: configured and OK
- Service: systemd user, running (pid 105849)
- Bind: loopback only — good for security
- ⚠️ Service PATH includes version manager dirs (`~/.nvm/...`) — recommended to fix via `openclaw doctor`
- ⚠️ Reverse proxy headers not trusted (but OK since loopback-only)

**OpenClaw Security Audit (--deep):**
- WARN: Telegram groupPolicy="open" — recommend allowlist mode
- WARN: trusted_proxies_missing — loopback-only so fine, but noted
- WARN: multi-user heuristic detected (Telegram groupPolicy=open, tools exposed without sandbox)
- WARN: Plugin `@openclaw/brave-plugin` has unpinned npm spec
- INFO: tools.elevated enabled, browser control enabled, trust model=personal assistant

**Cron Health:**
- All 10 cron jobs healthy, next run times reasonable
- "Mitty Security Audit" cron ran 4m ago, next in 1d
- "Bitty Budget Update" next in 26m
- "Mitty Health Pulse" next in 39m, ran 21m ago
- "Credential Leak Scanner" next in 3h, running every 6h

**RAM Check:** 6.7Gi total, 2.6Gi used, 1.2Gi free, 4.0Gi available. Swap 19Gi total, 23Mi used. Healthy.

**Filesystem Check:** /dev/nvme0n1p6 80G total, 44G used, 32G available (58% used). /boot/efi 256M, 38M used (15%). Healthy.

### ⚠️ Action Items
1. Run `openclaw doctor --repair` to fix gateway service PATH warnings
2. Consider tightening Telegram groupPolicy to allowlist
3. Pin `@openclaw/brave-plugin` to exact version in plugin index
4. If multi-user access is intended, enable sandbox mode

---
